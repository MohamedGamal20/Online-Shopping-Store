@model IEnumerable<Online_Shopping_Store.Models.Category>

<ul id="categories">

    @foreach (var category in Model)
    {
        <li>
            @Html.ActionLink(category.Name,"Brose", "Store",new  { Category = category.Name},null)
        </li>
    }
</ul>
